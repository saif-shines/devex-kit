# In progress

Draft skills live here.

A draft is not shipped. Do not put it under `plugins/<plugin>/skills/`.
Claude plugins and Codex plugins only load the plugin skill tree.

Promote a draft by moving it into the live plugin tree and completing the root-contract promotion checklist.

Do not install a draft with `npx skills add --skill` until it is promoted.
